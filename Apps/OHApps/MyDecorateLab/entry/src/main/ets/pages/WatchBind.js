"use strict";
// @Watch用于监听状态变量的变换，通过@Watch注册一个回调方法Onchanged，
// 当状态变量count被改变时，触发Onchange回调
struct WatchBind extends   {
    constructor(// @Watch用于监听状态变量的变换，通过@Watch注册一个回调方法Onchanged，
    // @Watch用于监听状态变量的变换，通过@Watch注册一个回调方法Onchanged，
// 当状态变量count被改变时，触发Onchange回调
    ) {
    }
    // Watch监听的状态变量一发生改变，就会调用它此函数
    OnNumChange() {
        this.Num2 += 2;
    }
    build() {
            .width('100%')
            .height('100%')
            .backgroundColor(0x1e90ff);
    }
}
function textform1() {
    
  
        .fontSize(20)
        .fontColor(0xffffff)
        .fontWeight(700)
        .margin({ top: 10, bottom: 10 });
}
function but1form1() {
    
  
        .backgroundColor(0xff1493)
        .width(220)
        .margin({ top: 30, bottom: 30 });
}
//# sourceMappingURL=WatchBind.js.map